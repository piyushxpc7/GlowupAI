[build-system]
requires = ["setuptools>=83.0"]
build-backend = "setuptools.build_meta"

[project]
name = "glowupai"
version = "3.0.0"
description = "A complete privacy-conscious personal appearance evidence workspace"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.6",
  "uvicorn[standard]>=0.34.0",
  "pillow>=11.0.0",
  "opencv-python>=4.10.0.84,<6.0.0",
  "numpy>=2.0.0",
  "cryptography>=44.0.0",
  "google-genai>=1.0.0",
  "psycopg[binary,pool]>=3.2.6",
  "PyJWT[crypto]>=2.10.0",
  "redis>=5.2.1",
  "sentry-sdk[fastapi]>=2.20.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "pytest-cov>=4.1.0",
  "pytest-asyncio>=0.21.0",
  "httpx>=0.27",
  "black>=23.0.0",
  "mypy>=1.5.0",
  "bandit>=1.7.5",
  "safety>=2.3.0",
  "locust>=2.15.0"
]
otel = [
  "opentelemetry-api>=1.20.0",
  "opentelemetry-sdk>=1.20.0",
  "opentelemetry-instrumentation-fastapi>=0.41b0",
  "opentelemetry-exporter-otlp>=1.20.0"
]

[project.scripts]
glowupai = "glowupai.cli:main"

[tool.setuptools.packages.find]
include = ["glowupai*"]

[tool.setuptools.package-data]
glowupai = ["static/*.html", "static/*.css", "static/*.js", "migrations/*.sql"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "unit: Unit tests",
  "integration: Integration tests",
  "slow: Slow tests (load tests)",
]
addopts = "--strict-markers -v"
asyncio_mode = "auto"

[tool.coverage.run]
source = ["glowupai"]
omit = [
  "*/tests/*",
  "*/venv/*",
  "*/__pycache__/*",
]

[tool.coverage.report]
precision = 2
show_missing = true
skip_covered = false

[tool.black]
line-length = 88
target-version = ['py311', 'py312']
include = '\.pyi?$'

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
ignore_missing_imports = true
